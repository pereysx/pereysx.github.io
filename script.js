$(document).ready(function () {
    // Включаем tooltip у всех элемента
    $("[data-toggle='tooltip']").tooltip();
    // Включаем popover у всех элемента
    $("[data-toggle='popover']").popover();
});